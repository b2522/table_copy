# **离线麒麟 V10 SP1（aarch64 / 麒麟 9000C）**的完整安装方案

## 一、需要装什么（依赖清单）

| 类别       | 名称         | 是否必需           | 说明                                  |
| ---------- | ------------ | ------------------ | ------------------------------------- |
| 解释器     | Python 3     | 必需               | 麒麟 V10 SP1 桌面版自带（通常是 3.7） |
| 包管理     | pip          | 必需               | 对应 `python3-pip`                    |
| GUI 库     | **PyQt5**    | **必需**           | 界面渲染，体积最大、最挑架构          |
| Excel 读写 | **openpyxl** | **必需**           | 读/写 .xlsx                           |
| .xls 支持  | xlrd         | 仅读 .xls 时才需要 | 纯 Python，很小；若只用 .xlsx 可跳过  |
| 系统库     | libGL 等     | 通常自带           | Qt 渲染需要，缺了才装                 |

> `sys / os / re / json / datetime` 都是 Python 标准库，无需安装。

## 二、⚠️ 最关键的死线：架构必须一致
你的麒麟 9000C 是 **ARM64（aarch64）**。所以**所有安装包都必须是 aarch64 版**，从 x86_64 机器（比如你这 Windows）直接下载的 PyQt5 包**拿过去用不了**。两条路：
- **首选**：用麒麟**本机安装 ISO**（离线介质，自带 aarch64 仓库）装 → 天然匹配。
- 次选：在一台 aarch64 联网机（或 aarch64 虚拟机/那台麒麟临时联网一次）里下载依赖，再用 U 盘拷过去。

## 三、推荐方案 A：用麒麟安装 ISO 做离线 apt 源（最省事、零架构烦恼）
你手上应该有装机用的 **Kylin V10 SP1 系统 ISO**，它就是个完整的本地仓库，不需要任何网络。

```bash
# 1) 把 ISO 拷到麒麟本机（U 盘），挂载
sudo mkdir -p /mnt/kyliniso
sudo mount -o loop /path/to/Kylin-SP1-*.iso /mnt/kyliniso

# 2) 注册为本地软件源（比手改 sources.list 可靠）
sudo apt-cdrom -m -d /mnt/kyliniso add
sudo apt-get update

# 3) 安装依赖
sudo apt-get install -y python3 python3-pip python3-pyqt5 python3-openpyxl
# 若需要读 .xls：
sudo apt-get install -y python3-xlrd
# 防止界面报 libGL.so.1 缺失：
sudo apt-get install -y libgl1-mesa-glx libxkbcommon0 libfontconfig1
```
装完直接 `python3 wps_table_mapper.py` 即可运行。

## 四、方案 B：U 盘带 wheels 离线 pip 安装
如果你拿不到系统 ISO，就用一台**同架构 aarch64 联网机**（或让那台麒麟临时联网一次）把包装进 U 盘：

```bash
# 在 aarch64 联网机上（已装好同版本 python3/pip）：
mkdir -p wheels
pip3 download pyqt5 openpyxl xlrd -d wheels/    # 自动下载 aarch64 版
# 把 wheels/ 拷到 U 盘，插到离线麒麟
```
到离线麒麟上：
```bash
# 用项目里已给的 offline_install.sh（或直接）：
pip3 install --no-index --find-links=/mnt/u盘路径/wheels pyqt5 openpyxl xlrd
```
> 我之前生成的 `download_deps.sh` / `offline_install.sh` 就是这套流程的脚本版，在 aarch64 机上跑 `download_deps.sh` 下载、`offline_install.sh` 安装。

## 五、验证装好了
```bash
python3 -c "import PyQt5, openpyxl; print('PyQt5', PyQt5.__version__, '| openpyxl', openpyxl.__version__)"
python3 wps_table_mapper.py
```
能弹出窗口、状态栏显示「已加载源表：XX 列」即成功。

## 六、更省心的提醒
如果这台麒麟要**长期离线、还可能换机器部署**，最干净的做法其实是**在麒麟本机跑一次 `build_kylin.sh`**（之前给你的脚本），打出单个可执行文件 `dist/WPS表格字段映射工具`——里面已经把 Python+PyQt5+openpyxl 全部打包进去，**目标机零依赖、不用装任何东西**，U 盘拷过去双击就跑。这比在每台离线机上都装一遍依赖省事得多。

---

**一句话总结**：离线麒麟最稳的是「拿装机 ISO 挂本地源 → `apt install python3-pyqt5 python3-openpyxl`（要读 .xls 再加 `python3-xlrd`）」。所有包务必 aarch64 版，别用 x86 机器下载。需要我把方案 A 的 ISO 离线安装也写成一个一键脚本（`offline_apt_iso.sh`）吗？
