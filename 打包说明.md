# WPS 表格字段映射工具 · 麒麟 Linux 打包说明

## 一、重要前提：必须在 Linux 上打包

PyInstaller **不能跨平台编译**。在 Windows 上无法直接生成 Linux 可执行文件，
必须在 Linux 环境（**推荐直接在目标麒麟系统上**，或 glibc 版本兼容的 Linux）执行打包。

- 本文档配套脚本：`build_kylin.sh`
- 入口程序：`wps_table_mapper.py`

## 二、在麒麟系统上的步骤

1. 把整个项目文件夹（`wps_table_mapper.py` + `build_kylin.sh`）复制到麒麟机器。
2. 打开终端，进入该目录，赋予执行权限并运行：

   ```bash
   chmod +x build_kylin.sh
   bash build_kylin.sh
   ```

3. 脚本会自动：建虚拟环境 → 安装 PyQt5/openpyxl/xlrd/PyInstaller → 打包。
4. 成功后生成单文件可执行程序：

   ```
   dist/WPS表格字段映射工具
   ```

5. 把该文件拷到同架构（amd64 / aarch64）的麒麟机器即可双击运行。

## 三、运行前可能需要的系统库

PyInstaller 已把 Qt/Python 运行时打进包里，但以下系统库通常需目标机自带
（麒麟桌面版一般已具备）：

- `libgl1`（Qt 窗口渲染所需，若启动报 `libGL.so.1` 缺失）：
  ```bash
  sudo apt-get install -y libgl1-mesa-glx libxkbcommon0 libfontconfig1
  ```

## 四、常见问题

| 现象 | 原因 / 解决办法 |
|------|----------------|
| `GLIBC_2.xx not found` | 打包机的 glibc 比目标机新。**直接在目标麒麟系统上重新跑 `build_kylin.sh`** 即可。 |
| 双击无反应、无窗口 | 终端执行 `./dist/WPS表格字段映射工具` 看报错；多半缺 `libgl1`，按上表安装。 |
| 中文显示方框/乱码 | 目标机缺中文字体，安装 `fonts-wqy-microhei` 或 `fonts-noto-cjk`。 |
| 想看运行日志排查 | 重新打包时去掉脚本里的 `--windowed` 参数。 |
| 包体太大（~100MB+） | 改用 `--onedir`（文件夹形式）更小、启动更快，把脚本中 `--onefile` 改为 `--onedir`。 |

## 五、更便携的方案：AppImage（可选）

若需要在多台不同麒麟/国产 Linux 间分发，可把 `dist` 产物再封装成 AppImage：

```bash
# 在麒麟上安装 linuxdeploy 后执行（需联网下载一次）
wget https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
chmod +x linuxdeploy-x86_64.AppImage
./linuxdeploy-x86_64.AppImage --appdir=AppDir --output=appimage
```

AppImage 自带运行依赖，跨同架构发行版兼容性更好。

## 六、架构一致性提醒

- 在 **x86_64（amd64）** 麒麟上打包的二进制，**不能**直接跑在 **arm64（aarch64，如飞腾/鲲鹏版麒麟）** 上，反之亦然。
- 请在与最终运行机器**相同 CPU 架构**的环境里打包。
